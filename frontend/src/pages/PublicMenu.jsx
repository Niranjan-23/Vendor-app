import { Typography } from '@mui/material';

export default function PublicMenu() {
  return (
    <div style={{ padding: '1rem' }}>
      <Typography variant="h6" align="center">
        Public Shop Menu (Scanned via QR)
      </Typography>
    </div>
  );
}
